apiVersion: v1
kind: Service
metadata:
  name: microservicio-eva3-service
  labels:
    app: fastapi-app
spec:
  type: LoadBalancer # Permite acceso externo si usas una nube pública
  selector:
    app: fastapi-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000